import 'package:flutter/material.dart';

const defaultPadding = EdgeInsets.fromLTRB(8, 8, 0, 0);
const defaulTextStyle = TextStyle(fontSize: 20);
const defaultPaddingAll = EdgeInsets.all(8);
const appTitle = 'dummyJSON';
